type Delivery {
    documentId: ID!
    driver: User!
    order: Order!
    delivered: Boolean
}

input DeliveryInput {
    documentId: ID!
    driver: UserInput!
    order: OrderInput!
}

input DeliveryEditInput {
    documentId: ID!
    driver: UserEditInput!
    order: OrderEditInput!
    delivered: Boolean
}